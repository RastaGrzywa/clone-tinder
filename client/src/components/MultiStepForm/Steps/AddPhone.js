import React from 'react';

import "./StepStyles.css";

const AddPhone = ({ newPosition }) => {
  return (
    <div className={`page ${newPosition}`}>
      <h1>PHONE</h1>
    </div>
  )
}

export default AddPhone
