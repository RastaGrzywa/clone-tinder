import React from 'react';

import "./StepStyles.css";

const AddEmail = ({ newPosition }) => {
  return (
    <div className={`page ${newPosition}`}>
      <h1>My email is</h1>
    </div>
  )
}

export default AddEmail
