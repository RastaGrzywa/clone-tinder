import React from 'react';

import "./StepStyles.css";

const AddBirthday = ({ newPosition }) => {
  return (
    <div className={`page ${newPosition}`}>
      <h1>My birthday is</h1>
    </div>
  )
}

export default AddBirthday
